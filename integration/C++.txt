String incoming = "";

void setup() {
  Serial.begin(115200);
}

void loop() {
  while (Serial.available()) {
    char c = Serial.read();
    incoming += c;

    if (c == '>') {
      processPacket(incoming);
      incoming = "";
    }
  }
}

void processPacket(String packet) {
  if (packet.indexOf("MOVE:FORWARD") > 0) {
    // drive motors forward
  }
}
still learning c++